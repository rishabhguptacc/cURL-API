<?php

echo "<h2>View for ISS-API data.</h2>";
// echo '<br>'. $response;

echo '<pre>';
print_r('<br>'.$response.'<br><br>');
// print_r(xmlrpc_decode($response));        //  Since the output isn't json, json_decode doesn't works!
//  die;

echo $this->tag->linkTo(
    ['/',
    'Back',
    'class' => 'btn btn-primary']
);
